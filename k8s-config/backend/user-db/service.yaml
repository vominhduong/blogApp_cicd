apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: production
  labels:
    app: user-db
spec:
  selector:
    app: user-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
